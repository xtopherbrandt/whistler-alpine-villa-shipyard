---
config_version: 3
project_name: "Whistler Alpine Villa"
project_type: "web-app"
tech_stack:
  - Next.js
  - TypeScript
  - React
testing_framework: "vitest"
e2e_framework: "playwright"
test_commands:
  unit: "vitest run"
  integration: "vitest run --config vitest.integration.config.ts"
  e2e: "playwright test"
  scoped: "vitest run -t"
build_commands:
  scoped: ""
  full: "npm run build"
ci_platform: "github-actions"
repo_type: single
team_size: solo
workflow: sprint
git:
  main_branch: main
  commit_format: conventional
  commit_scope: true
  commit_case: lowercase
  commit_examples:
    - "feat(ui): add property listing carousel"
    - "fix(booking): handle date overlap validation"
    - "chore: update dependencies"
wip_limits:
  discuss: 3
  approved: 10
  in_progress: 3
  review: 5
staleness:
  warning_age: 60
  critical_age: 120
operational_tasks:
  max_iterations: 3
  max_patch_tasks: 5
execution:
  max_parallel_agents: 3
---

# Project Configuration

This file is generated by `/ship-init`. Edit directly or re-run `/ship-init` to update.
