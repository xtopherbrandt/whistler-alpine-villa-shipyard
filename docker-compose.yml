services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: whistler
      POSTGRES_PASSWORD: whistler
      POSTGRES_DB: whistler_alpine_villa
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
